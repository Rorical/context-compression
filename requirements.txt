# Context Compression Training System - Dependencies
# Python 3.10+ recommended.
#
# Install the base dependencies with:
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Unsloth is intentionally NOT listed here because its dependency constraints
# can conflict with the newest Hugging Face stack on Colab. Install Unsloth
# separately for the training environment you actually use.

# Core training stack
torch>=2.10.0
transformers>=5.3.0
datasets>=4.7.0
accelerate>=1.13.0
numpy>=1.26.0
tqdm>=4.67.0
pyyaml>=6.0.2

# RL / PEFT
trl>=0.29.1
peft>=0.18.1
mergekit>=0.1.4

# Evaluation
rouge-score>=0.1.2
sacrebleu>=2.6.0
bert-score>=0.3.13
nltk>=3.9.3
sentence-transformers>=5.2.3
tiktoken>=0.9.0

# Tracking and optional evaluators
wandb>=0.25.1
openai>=1.0.0
anthropic>=0.8.0

# Development
pytest>=8.3.0
black>=25.0.0
