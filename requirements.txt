# Context Compression Training System - Dependencies
# Python 3.10+ recommended. Unsloth currently supports Python 3.13 or lower.
#
# For Colab / Linux, install with:
#   pip install --upgrade pip
#   pip install --upgrade --force-reinstall --no-cache-dir unsloth unsloth_zoo
#   pip install -r requirements.txt

# Core training stack
torch>=2.10.0
transformers>=5.3.0
datasets>=4.7.0
accelerate>=1.13.0
numpy>=1.26.0
tqdm>=4.67.0
pyyaml>=6.0.2

# Unsloth
unsloth>=2026.3.18
unsloth_zoo>=2026.3.7

# RL / PEFT
trl>=0.29.1
peft>=0.18.1
mergekit>=0.1.4

# Evaluation
rouge-score>=0.1.2
sacrebleu>=2.6.0
bert-score>=0.3.13
nltk>=3.9.3
sentence-transformers>=5.2.3
tiktoken>=0.9.0

# Tracking and optional evaluators
wandb>=0.25.1
openai>=1.0.0
anthropic>=0.8.0

# Development
pytest>=8.3.0
black>=25.0.0
